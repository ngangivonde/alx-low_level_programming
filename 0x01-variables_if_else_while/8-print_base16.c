#include <stdio.h>

/**
 * main - Print base 16 numbers in lowercase
 * Return: Always 0 (success)
 */

int main(void)
{
	int i;

	for (i = 97; i < 103; i++)
		putchar(i);
	putchar('\n');
	return (0);
}
