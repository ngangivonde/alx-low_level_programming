#include "main.h"
/**
 * mul - Multiples two intergers
 * @x: the first integer to be multiplied with,
 * @y: the second interger to be multiplied
 * Return: the result of multiplication of x param and y param
 */
int mul(int x, int y)
{
	return (x * y);
}
